<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="none">
  <!-- Top line -->
  <rect x="10" y="20" width="80" height="8" fill="currentColor"/>

  <!-- Middle line (shorter to accommodate circle) -->
  <rect x="10" y="46" width="38" height="8" fill="currentColor"/>

  <!-- Circle on middle line (abacus bead) -->
  <circle cx="75" cy="50" r="11" fill="currentColor"/>

  <!-- Bottom line -->
  <rect x="10" y="72" width="80" height="8" fill="currentColor"/>
</svg>
